<nav class="navbar navbar-default">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <% if params[:controller] == 'events' && params[:action] == 'show' %>
        <a class="navbar-brand" href="<%= events_path %>"><i class="fa fa-chevron-left"></i></a>
      <% else %>
        <%= link_to "UCB", root_path, class: "navbar-brand", id: "logo" %>
      <% end %>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <li class="<%= 'active' if params[:controller] == 'events' %>"><%= link_to 'Events', events_path, method: :get %></li>
        <li class=""><a href="#">Check In</a></li>
        <li class=""><a href="#">My Diary</a></li>
        <li class="<%= 'active' if current_page?(help_path) %>"><%= link_to 'Help', help_path %></li>
        <li class="<%= 'active' if current_page?(about_path) %>"><%= link_to 'About Us', about_path %></li>
      </ul>

      <ul class="nav navbar-nav navbar-right">
        <li class="dropdown">
          <% if logged_in? %>
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">
            <i class="fa fa-user fa-fw"></i>&nbsp; 
            <%= @current_user.name.split.first %>
            <span class="caret"></span>
          </a>
          <ul class="dropdown-menu">
            <li><a href="#"><strong>575 Points</strong></a></li>
            <li role="separator" class="divider"></li>
            <li><%= link_to 'View Profile', current_user %></li>
            <li><%= link_to 'Edit Profile', '/users/' + current_user.id.to_s + '/personal' %></li>
            <li role="separator" class="divider"></li>
            <li>
              <%= link_to 'Sign out', logout_path, method: "delete" %>
            </li>
          </ul>
        </li>
        <% else %>
          <li><%= link_to "Log in", login_path %></li>
        <% end %>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>